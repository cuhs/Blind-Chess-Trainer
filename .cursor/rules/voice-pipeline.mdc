---
description: Voice match pipeline — STT, normalizer, disambiguation, integrity
globs: "packages/voice-pipeline/**"
alwaysApply: false
---

# Voice Pipeline

```
STT (mobile: expo-speech-recognition)
  → normalizeSpokenMove / pickBestTranscript
  → buildContextualStrings (position + disambiguation bias)
  → resolveMove | resolveDisambiguationVoice (chess-core)
  → Apply | DisambiguationOverlay
```

## Rules

- On-device STT primary (`requiresOnDeviceRecognition` when supported)
- `normalizeMove` is a pure, table-tested function in chess-core
- Disambiguation voice: `resolveDisambiguationVoice` first; tap uses `chooseDisambiguation`; failed voice falls through to `resolveMove`
- Stockfish WASM off main thread

## Homophones

night→N, rook→R, bishop→B, queen→Q, king→K (handled in `chess-core` `match-move.ts`)

## Mobile wiring

`useMatchSpeech` → `submitPlayerMove` in `useMatchSession`. See `voice-match` skill.
